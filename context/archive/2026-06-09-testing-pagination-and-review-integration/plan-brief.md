# Pagination and review integration — Plan Brief

> Full plan: `context/changes/testing-pagination-and-review-integration/plan.md`
> Research: `context/changes/testing-pagination-and-review-integration/research.md`

## What & Why

Reduce risk that list pagination or review-session scheduling regress and cause user-facing data loss, duplicates, or incorrect review behavior. The plan focuses on the cheapest tests that give real signal: an integration boundary test first, then grounding review scheduling.

## Starting Point

- Offset-based paging implemented in `src/pages/api/flashcards.ts` / `src/lib/services/flashcards.ts`.
- Sparse integration tests; one UI pagination visibility test exists (`src/components/FlashcardList.test.tsx`).
- Research file created at `context/changes/testing-pagination-and-review-integration/research.md` documenting hotspots and missing scheduling API.

## Desired End State

- Reliable integration tests that catch paging regressions at boundaries.
- Grounded review-session contract/tests ensuring due-cards ordering and scheduling persistence.
- Clear cookbook patterns for adding similar integration tests in future phases.

## Key Decisions Made

| Decision                       | Choice                                            | Why (1 sentence)                                       | Source     |
|-------------------------------|---------------------------------------------------|--------------------------------------------------------|------------|
| First sub-phase                | ⭐ Integration boundary test                       | Catches real-world edits that unit tests miss          | Research   |
| Use real DB fixtures           | Yes                                               | Integration tests need persistence to reproduce issues | Plan       |
| Review scheduling discovery    | Grounded search then contract tests               | No due-cards API found in research; must be validated  | Research   |

## Scope

**In scope:**
- Integration test that simulates edit/delete at page boundaries
- Discovery and contract tests for review-session scheduling
- Ownership/RLS contract checks as needed

**Out of scope:**
- Provider/infrastructure-specific Supabase/Cloudflare behavior
- Large e2e suite or broad UI automation beyond narrow integration tests

## Architecture / Approach

High-level: follow a test-first approach — write integration tests that exercise existing API/service layers with DB fixtures, then fix code or add minimal API contract if review scheduling lacks an entrypoint. Keep changes narrowly scoped to tests and small API contract additions.

## Phases at a Glance

| Phase     | What it delivers                                   | Key risk                  |
|-----------|----------------------------------------------------|---------------------------|
| 1. Integration boundary test | Integration test that reproduces and prevents pagination regressions | Test harness flakiness / fixture fragility |
| 2. Review scheduling grounding | Discovery + contract tests for due-cards and review result persistence | Scheduling code hidden or distributed |
| 3. Ownership/RLS tests | Contract tests ensuring user isolation | RLS misconfig or missing server-side checks |

**Prerequisites:** test DB + migrations runnable in CI; Vitest integration harness configured.
**Estimated effort:** ~2-3 developer-days across phases (Phase 1: 1 day; Phase 2: 1 day; Phase 3: 0.5-1 day)

## Open Risks & Assumptions

- Assumes a test DB and seed/migration scripts can run reliably in CI.
- If review scheduling code is distributed and requires schema changes, effort may increase.

## Success Criteria (Summary)

- Integration boundary test exists and is green in CI.
- Review-session contract tests validate ordering and persistence.
- No user-visible pagination/regression issues in manual spot checks.


Generated by Copilot on 2026-06-09
